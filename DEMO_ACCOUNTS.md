# 📋 JiaBaMap (呷飽地圖) 演示帳號清單

此文件包含用於系統功能展示的測試帳號及其對應的功能。目前系統已填充豐富的展示資料（包含歷史訂單、食記、留言與通知）。

## 1. 一般用戶 (消費者端)
這組帳號最適合展示：**地圖搜尋、線上訂餐、發表食記、按讚留言互動、通知中心。**

| 角色 | 帳號 (Email) | 密碼 | 展示內容 |
| :--- | :--- | :--- | :--- |
| **用戶 A (作者)** | `demo-user@jiabamap.dev` | `DemoUser123!` | 擁有熱門食記、數筆已完成訂單、以及來自用戶 B 的按讚與留言通知。 |
| **用戶 B (互動)** | `test@jiabamap.dev` | `Test1234!` | 擁有一筆「製作中」與一筆「待付款」訂單，可接收店家更新狀態的即時通知。 |

---

## 2. 店家帳號 (業者端)
這組帳號最適合展示：**接單通知、訂單管理、菜單編輯、營收統計、評價查看。**

| 角色 | 帳號 (Username) | 密碼 | 展示內容 |
| :--- | :--- | :--- | :--- |
| **咖啡館老闆** | `demostore` | `DemoStore123!` | 擁有實體店面「呷飽精品咖啡館」。已有 8 項菜單、多筆歷史訂單，以及用戶留下的星級評論。 |

---

## 3. 本機開發測試帳號 (In-Memory MongoDB)
當 `MONGO_URI` 未設定時，後端啟動自動 seed，帳號資訊會印在 console。

| 角色 | 帳號 | 密碼 | 說明 |
| :--- | :--- | :--- | :--- |
| **測試用戶** | `test@jiabamap.dev` | `Test1234!` | 一般消費者帳號 |
| **測試店家** | `teststore` | `Test1234!` | 完整店家資訊 + 6 項菜單（飲料 × 2、甜點 × 2、主食 × 2） |

> **提示**：storeToken 請從啟動後的 console 複製，貼入瀏覽器 `localStorage.storeToken`。

---

## 🚀 建議演示流程 (End-to-End)

### 1. 社交與互動演示 (Social Interaction)
*   **準備**：登入 **用戶 A**。
*   **操作**：點擊右上角「通知鈴鐺」。
*   **結果**：看到「展示用戶 B 按讚了你的文章」與「展示用戶 B 評論了你的文章」。
*   **深挖**：進入食記頁面，可看到多層級的留言與回覆互動。

### 2. 訂單生命週期演示 (Order Life Cycle)
*   **準備**：瀏覽器 A 登入 **店家帳號**，瀏覽器 B 登入 **用戶 B**。
*   **操作**：
    1.  **用戶 B** 的訂單清單中應有一筆「待付款」訂單。
    2.  **店家** 在 Dashboard 看到「新訂單」通知，點擊進入訂單管理。
    3.  **店家** 將該訂單狀態改為「製作中」。
*   **結果**：**用戶 B** 的畫面會立刻跳出即時推播，顯示「您的訂單狀態已更新」。

### 3. 餐廳評價與探索 (Discovery & Reviews)
*   **準備**：登入 **任一用戶**。
*   **操作**：在地圖上點擊「呷飽精品咖啡館」。
*   **結果**：在餐廳詳情中可看到 **用戶 A** 與 **用戶 B** 留下的真實星級評價、消費金額與照片。

---

## 🛠️ 展示資料重置
若需重新產生乾淨且完整的展示資料，請於後端目錄執行：
```bash
node seeds/demoSeeder.js
```
